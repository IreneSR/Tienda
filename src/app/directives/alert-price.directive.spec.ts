import { AlertPriceDirective } from './alert-price.directive';

describe('AlertPriceDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertPriceDirective();
    expect(directive).toBeTruthy();
  });
});
